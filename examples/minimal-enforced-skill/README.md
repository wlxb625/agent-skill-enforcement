# Minimal ASEP Enforced Skill

A small three-stage package used to demonstrate stage locks, deterministic validation, a semantic gate, repair routing, and receipt-backed completion.

Validate with:

```bash
asep validate examples/minimal-enforced-skill
```
