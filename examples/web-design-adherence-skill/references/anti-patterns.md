# Anti-patterns

The following patterns do not automatically satisfy the Skill:

- dark background plus large gradient headline;
- repeated rounded cards as the main composition;
- every section entering with the same fade and vertical offset;
- decorative parallax that does not change content relationships;
- full-screen media with unrelated text placed on top;
- removing the central experience on mobile without designing an alternative.
